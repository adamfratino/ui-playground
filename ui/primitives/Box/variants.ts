const section = {
  margin: "0 auto",
  maxWidth: "1224px",
  padding: "thick",
  width: "100%",
};

const outer = {
  display: "flex",
  flexDirection: "column",
  minHeight: "100vh",
  paddingY: 8,
  paddingX: 2,
  width: "100%",
};

const insetShadow = {
  boxShadow: "box.inset",
  backgroundColor: "rgba(0,0,0,0.4)",
  color: "white",
  padding: "thin",
  width: "fit-content",
};

const variants = {
  section,
  outer,
  insetShadow,
};

export default variants;
