import { toBreakpointsArray } from "~ui/helpers";

const variants = {
  title: {
    fontSize: toBreakpointsArray({
      sm: 48,
      md: 72,
      lg: 96,
    }),
    fontWeight: 700,
  },
  description: {},
};

export default variants;
