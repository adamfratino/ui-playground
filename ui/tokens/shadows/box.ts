const deep = `rgba(0, 0, 0, 0.48) 6px 2px 16px 0px, rgba(0, 0, 0, 0.8) -6px -2px 16px 0px`;
const inset = `rgba(0, 0, 0, 0.24) 0px 3px 8px inset`;
const shallow = `rgba(0, 0, 0, 0.24) 0px 3px 8px`;

const shadows = {
  deep,
  inset,
  shallow,
};

export default shadows;
