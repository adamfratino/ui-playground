import button from "./button";
import table from "./table";

/**
 * Define base CSS styles for HTML elements
 * @example `button.ts` will style `<button />`
 * @see https://theme-ui.com/theme-spec#styles
 */
const styles = {
  button,
  table,
};

export default styles;
