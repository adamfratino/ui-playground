const matches = {
  grid: {
    gridTemplateColumns: "repeat(6, 1fr)",
    zIndex: 100,
  },
};

const tournaments = {
  grid: {
    gridTemplateColumns: "1fr, 2fr, 1fr, 1fr, 1fr",
  },
};

const variants = {
  matches,
  tournaments,
};

export default variants;
